spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://ep-damp-violet-ax1b2bk1-pooler.c-4.us-east-2.aws.neon.tech:5432/neondb?sslmode=require&channel_binding=require
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  graphql:
    http:
      path: /api/graphql
    graphiql:
      enabled: true
      path: /api/graphiql
    cors:
      allowed-origins:
        - https://dev.andrey-evtukh.vercel.app
        - https://andrey-evtukh.vercel.app
      allowed-methods:
        - GET
        - POST
        - OPTIONS
      allowed-headers: "*"
      allow-credentials: true

server:
  port: 8080

resend:
  api-key: ${RESEND_API_KEY}
  from: ${MAIL_FROM:andrey.evtukh@gmail.com}

jwt:
  secret: ${JWT_SECRET:436a2692f8e7fb78240068bb110d87f5222a8486c7f98fd06884e71a2b3178c9}
  expiration-ms: 3600000   # 1 час

app:
  frontend-url: http://localhost:4200   # для ссылки сброса пароля
  verification-code-ttl-minutes: 5

logging:
  level:
    org.springframework.graphql: DEBUG
    org.springframework.web: DEBUG
    org.springframework.http: TRACE
