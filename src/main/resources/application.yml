spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://ep-damp-violet-ax1b2bk1-pooler.c-4.us-east-2.aws.neon.tech:5432/neondb?sslmode=require&channel_binding=require
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  graphql:
    http:
      path: /api/graphql
    graphiql:
      enabled: true
      path: /api/graphiql
    cors:
      allowed-origins:
        - https://dev.andrey-evtukh.vercel.app
        - https://andrey-evtukh.vercel.app
      allowed-methods:
        - GET
        - POST
        - OPTIONS
      allowed-headers: "*"
      allow-credentials: true

  mail:
    test-connection: true
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:andrey.evtukh@gmail.com}
    password: ${MAIL_PASSWORD:zwwk rnej fiys qgbm}
    properties:
      mail:
        debug: true
        smtp:
          auth: true
          connectiontimeout: 10000
          timeout: 10000
          writetimeout: 10000
          starttls:
            enable: true
            required: true

server:
  port: 8080

mail:
  host: ${spring.mail.host}
  port: ${spring.mail.port}
  username: ${spring.mail.username}
  password: ${spring.mail.password}

jwt:
  # Секрет тоже из переменной окружения. Значение ниже — только дефолт для локальной разработки.
  secret: ${JWT_SECRET:this-is-a-dev-only-secret-change-it-please-32bytes}
  expiration-ms: 3600000   # 1 час

app:
  frontend-url: http://localhost:4200   # для ссылки сброса пароля
  verification-code-ttl-minutes: 15

logging:
  level:
    org.springframework.graphql: DEBUG
    org.springframework.web: DEBUG
    org.springframework.http: TRACE
    jakarta.mail: DEBUG